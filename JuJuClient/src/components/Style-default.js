import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    Layout: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      },
      title: {
        height: 120,
        alignItems: 'center',
        justifyContent: 'center',
      },
      content: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      },
});